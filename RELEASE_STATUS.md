# Release status

The earlier v1.0.0/v1.0.1 completion wording was withdrawn after the deeper routine-level audit and remains historical only.

RE #6 closes that strengthened audit. The current tree is the first checkpoint that satisfies the project's final readable-C completion criterion: active retail routine-level closure plus strict GCC/Clang builds, regression tests, whole-archive linking, unfinished-marker scanning, publication-boundary auditing, and clean-archive revalidation.

This status does not change the claim boundary in `STATUS.md`: the high-level C tree is a faithful readable semantic reconstruction, while the separate preservation repository remains the byte-exact binary oracle.
