# Superseded unzip research

This directory preserves earlier compatibility sketches used while reconstructing the ROM bootstrap. They are **not** part of the active readable-C build.

The active implementations are `src/unzip/alloc_reconstructed.c` and `src/unzip/inflate_reconstructed.c`.
